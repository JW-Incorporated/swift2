# Architecture (stub — to be written)

Deliberately empty for now. Partner (CTO) owns this file.

To be decided and documented here (each gets a decision-log entry):

- Stack (leading default for web + mobile with a two-person AI-first team:
  React / React Native or Expo, TypeScript — decide, don't assume)
- Hosting and deployment
- Database and auth
- AI-integration approach (which APIs, where keys live, cost controls)
- Coding standards (once stack is chosen, Claude generates a first draft
  for Partner's approval)

Until this is filled in, AI agents must not make irreversible technical
choices — propose options with tradeoffs instead.
