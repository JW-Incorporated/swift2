# Wave M1 — Comms: Discord delivery, the rebuilt brief, email retired

Paste everything below this line into a fresh **Sonnet** session in Swift2. Prerequisites: M0 merged (#4184, #4183, #4185); **HA #66 done** (`gh api repos/JW-Incorporated/swift2/environments/ops/secrets` lists `DISCORD_MARJORIE_WEBHOOK_URL`). If #66 is still open, build everything, but stop before the "Done means" live checks and say so — do not fake them.

---

You are executing Wave M1 of the Marjorie Overhaul (`docs/plans/marjorie-overhaul/PLAN.md`, epic #4180). Read `PLAN.md` (especially "M0 findings that bind M1"), then `docs/specs/marjorie-overhaul/c1-delivery.md`, `c2-brief.md`, `c3-email-retired.md` in full — they are the source of truth; this brief only sequences them. Then read the two M0 closeout comments on #4180 (2026-09-12 16:36 and 17:02). The owner authorizes up to 6 concurrent subagents (drop to 3 if the limit climbs). Write a `PLAN.md`-style step list per task for the `executor`, in the task's own worktree outside `Documents\Claude\Projects\`, then run `executor`; you orchestrate, review, confirm real CI with your own `gh pr checks`, merge, exit.

**Hard rules carried in:** never discard uncommitted work (no `restore`/`checkout --`/`reset --hard`/`clean`); never run `scripts/social/post-queue.mjs` or `delete-media.mjs`; never hand-write an `approval` object; never merge a PR touching `social/queue/**.json`; never post to `#longlive-tree`, `#longlive`, or `#human-action-*` from anything Marjorie; `gh secret`/`gh variable` are human-only → `HUMAN-ACTIONS.md` (format v2, `human-actions` skill); agent jobs never see `DISCORD_BOT_TOKEN` or the webhook — the write credential lives only in an `environment: ops` job; you confirm real CI yourself before merging; "auto-merge armed" is not "merged" — check PR state before reporting; files under 300 lines (`assemble-brief.mjs` is 739 and must **shrink**; `watchdog.yml` is ~1,100 and must shrink); a `MAP.md` row for every new file; docs updated in the same PR that changes behavior. Codex review (`codex:rescue --background`, results via `codex-companion.mjs result <id>`) on every PR touching `watchdog.yml`, `upsert-alert.sh`, or `routine-template.yml`; max two rounds, then `DEBUG.md` and the debug ladder.

## Step 0 — reconcile PR #4047 before any Discord code (you, 20 min)

PR #4047 "Deliver the morning brief to Discord" already implements chunking, mention suppression, retry, a Gmail fallback and watchdog classification in `scripts/discord/post-brief.mjs` (+15 tests) and `delivery-status.mjs`. It is `CONFLICTING` against `main`, keeps `brief-mailer.yml` (which C3 deletes), uses a repo-level secret `DISCORD_BRIEF_WEBHOOK` (which does not exist — HA #50 was closed without it), and targets the wrong channel. Read its two source files and its tests. **Lift** what is good (test cases, retry shape, the delivered/unconfigured/failed classification) into the C1 module; **keep** C1's `ops`-environment secret and two-job delivery shape; **drop** everything hanging off `brief-mailer.yml`. Then close #4047 with a comment pointing at your C1 PR (or rebase it into C1 — whichever leaves less dead code). Do not leave it open competing with M1. Say in the C1 PR body exactly what was lifted.

## Step 1 — the `environment` input on `routine-template.yml` (gates everything)

`routine-template.yml` has no `environment:` key, so no routine job can read an `ops` secret. Add an optional `environment` input with an empty default and prove the empty default is a no-op on an existing routine (dispatch one, e.g. `routine-marjorie-brief.yml`, and confirm it runs as before). Codex review, merge, then proceed. If GitHub rejects an empty `environment:` string, use a second job instead and document that in `c1-delivery.md` — the brief already delivers from a second job (`needs: run`, `environment: ops`, pattern at `routine-tree-weekly-plan.yml:83-101`), so this step only decides whether `upsert-alert.sh` callers can do the same in-job.

## Tasks (parallel where independent, after Step 1)

- **C1 delivery** — `scripts/marjorie/lib/discord.mjs` (`post(text, {thread?})`, one retry, chunking and mention-neutralising **imported** from `scripts/community/discord-delivery.mjs`, never copied), `scripts/marjorie/post-or-mail.mjs` (CLI: Discord, then on a second non-2xx `send-mail.py` with subject prefix `[discord failed]`), tests per the spec's list plus the lifted #4047 cases. Prove it live: one real `post-or-mail.mjs` dispatch from an `environment: ops` job lands in `#longlive-marjorie` as "Marjorie".
- **C2 brief** — rebuild `assemble-brief.mjs` to the six sections in the spec's exact template order (Waiting on you / Since yesterday / Today / Site / Tree / Distance to done), 40-line cap with truncate-and-say-so, new `lib/tree-line.mjs` and `lib/submissions.mjs`, Distance to done scored against `docs/definition-of-done.md`'s eight items (not `CLAUDE.md`, not the 12 retired gates). Update `assemble-brief.test.ts` (it asserts the five old headings) in the same PR. Delivery is a second job in `routine-marjorie-brief.yml` (`needs: run`, `environment: ops`) calling `post-or-mail.mjs`; the GitHub issue stays the durable copy; keep the `cc` first line the two other consumers key on. Update `runner-prompts/marjorie-brief.md` and `marjorie-delta.md`. **Do not build the reply poller** — that is M2 (moved from M4 per the M0 closeout).
- **C3 email retired** — per the spec's Files-affected table: delete `brief-mailer.yml`, `marjorie-inbox.yml`, `send-community-mail.py`; `upsert-alert.sh` tail becomes Discord-by-default with `ALERT_ALSO_MAIL` opt-in and **posts on state change only** (watchdog is hourly and re-opens standing alerts every pass — 168 posts a week is a failure); `production-backup.yml` sets `ALERT_ALSO_MAIL: 1` on its two alert steps; all **six** `upsert-alert.sh` callers (`watchdog`, `production-backup`, `backup-restore-drill`, `production-backup-drill`, `mobile-parity`, `social-audit`) get `environment: ops` on the alerting job or they silently keep mailing; `tree-mail.yml` loses its two send steps; `social-poster.yml` drops the success notice and routes the failure notice through `upsert-alert.sh`; `watchdog.yml` drops the `:86-107` catch-up block and `marjorie-inbox.yml` from its watch list. **Do not** file a human action to delete the Gmail secrets — `community-inbox.yml` still reads them over IMAP. Update `docs/agents/runners.md`, `docs/AUTOMATION.md`.

Order: Step 1 → C1 → (C2 ∥ C3). C2 and C3 both touch `runner-prompts/marjorie-brief.md`; C3 rebases on C2.

## Done means

Every gate below proven by a command you ran, pasted in the closeout comment on #4180: a real scheduled or dispatched `routine-marjorie-brief.yml` run posted the brief to `#longlive-marjorie` (run log shows the POST 2xx); `git grep send-mail.py .github/workflows` returns only `upsert-alert.sh`'s opt-in tail (reached by `production-backup.yml`) and the `post-or-mail.mjs` fallback; `marjorie-inbox.yml` and `brief-mailer.yml` gone from `main`; a forced watchdog alert (the spec's synthetic path — break nothing real) appeared in-channel once, not once per hour; #4047 closed or merged; `MAP.md` rows for every new file. Then **set MR1 and MR2 `due` dates in `checkpoints.json`** (+7 and +21 days from the merge date) in the same closeout PR — a null `due` never fires. Tick M1 on #4180, update `STATE.md`, stop.
