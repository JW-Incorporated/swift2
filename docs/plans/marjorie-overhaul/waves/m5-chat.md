# Wave M5 — Talk to the bots (Discord chat)

Paste everything below this line into a fresh **Opus** session (`/model opus`) in Swift2. Prerequisites: M4 merged (done 09-13); HA #69 done (closed 09-13 — the bot already had channel access; reply-poll run 34774300277 read `#longlive-marjorie` with no 403); no Tree wave open. If a reaction or thread-create call 403s during the build, file a new human action naming the exact missing permission — do not reopen #69. The spec is already written; this session builds and proves it.

---

You are executing Wave M5 of the Marjorie Overhaul (`docs/plans/marjorie-overhaul/PLAN.md`, epic #4180): the Discord chat loop, spec `docs/specs/marjorie-overhaul/m5-chat.md` (read it in full first — it is the contract; if the build must deviate, amend the spec in the same PR and say why in one line). Also read `docs/agents/marjorie.md`, `docs/agents/tree.md`, `docs/agents/routine-invariants.md`, `scripts/marjorie/reply-poll.mjs` (reuse its fetch, backoff and message filters), `.github/workflows/routine-template.yml`, `routine-marjorie-ops.yml` (the `expose_dispatch_token` pattern) and `docs/specs/marjorie-overhaul/l1-loop.md` (how the bots already talk to each other — both chat prompts must be able to explain it).

**Hard rules carried in:** the M1 list (`waves/m1-comms.md`). `DISCORD_BOT_TOKEN` and every webhook URL live only in `run:` steps under `environment: social`/`ops`; the agent never sees them (the PR diff is the proof). `#longlive-tree` approvals stay reaction-only — a chat reply is never an approval and never touches `social/queue/`. `social/lessons.md` is read-only to both chat routines. The 09-14 Monday cycle is the L1 proof MR1 checks: do not change `tree-weekly-plan.md`, `weekly-brief.mjs`, `assemble-brief.mjs` or `loop-asks.mjs` in this wave. Codex review (`codex:rescue --background`, results via `codex-companion.mjs result <id>`) on every PR that adds a workflow or touches `routine-template.yml`, max two rounds. Charter edits to `tree.md`/`marjorie.md` merge on green (#4185 rule).

**Carried in from M2–M4 (2026-09-13 reviews):** the agent's `gh` identity is `claude[bot]` (App installation token) and a `run:` step's is `GITHUB_TOKEN` — never rely on `viewerDidAuthor`; the workflow's `permissions:` block does not reach the agent (#4223), so dispatching from inside the agent needs `GH_TOKEN="$GH_DISPATCH_TOKEN"`, while dispatching from a `run:` step with `actions: write` just works. `gh issue list --json comments` truncates (#4230). GitHub list endpoints lag a fresh write by seconds (#4260) — that is why this wave's idempotency is a Discord reaction added *before* dispatch, not a GitHub lookup. The founder's 2026-09-13 reply to the brief was a Discord *reply* (a `message_reference` in the channel), not a thread — `reply-poll.mjs` only looks for a thread on the brief message, so it logged "no thread found yet" and did nothing; `chat-poll.mjs` must treat such replies as top-level founder messages and answer by creating a thread on the founder's own message. Size prompts to one message per run; do not raise turn budgets blindly (#4221). Work in a worktree outside `Documents\Claude\Projects\`; this checkout may be locked by another session.

## Tasks (in order; one PR each unless trivially coupled)

1. **Template + poll** — `routine-template.yml` gains the optional artifact pre-step (default off, existing callers unaffected — prove with the existing workflow YAML tests / a dry parse). `scripts/marjorie/chat-poll.mjs` + tests; `bot-chat-poll.yml` (cron `*/5`, `social` env, `actions: write`, kill switch `BOT_CHAT_ENABLED`). Dispatch with the poll's own `GITHUB_TOKEN`.
2. **Marjorie chat** — `routine-marjorie-chat.yml`, `docs/agents/runner-prompts/marjorie-chat.md` (act first, then answer; the authority list from the spec §Mechanics 4 verbatim; cite numbers; ≤1800 chars), `scripts/marjorie/chat-post.mjs` + tests (thread create, webhook post, ✅/❌, `[chat failed]`, the `💬 chat:` turn-log comment), `docs/agents/marjorie.md` amendment. Smoke: dispatch the poll after posting a real test message yourself is not possible from Actions — ask Joey in one line to type `what is your job?` in `#longlive-marjorie` (YOU:), then watch the poll and the routine run.
3. **Tree chat** — `routine-tree-chat.yml`, `tree-chat.md` (read-mostly; proposals as plan-PR comments; explains L1), `docs/agents/tree.md` amendment ("never posts to social platforms and never approves" replaces "never post, never reply"). Same live proof in `#longlive-tree`.
4. **Power proof** — ask Joey (YOU:) to tell Marjorie in chat that one currently listed blocker is done (his call which); her reply must cite the human-action PR or the closed issue. If she only talks and does not act, that is a prompt bug — fix and re-prove, it is the wave's point.
5. **Docs** — `docs/decisions.md` entry, `MAP.md` rows, `checkpoints.json` MR2 gains "≥1 real chat turn per bot with ✅", `HUMAN-ACTIONS.md` #69 closed by Joey's `done` (do not close it yourself).

## Done means

Both bots answered a real founder message in-thread with ✅ within 15 minutes, both said in their own words what their job is and how they reach each other, and Marjorie's power proof cites a real PR or closed issue — message links, run URLs and numbers on #4180. Unit tests green, `npm run typecheck --workspace=@swift2/web` untouched-green, no secret in an agent environment. Tick M5 on #4180 and in `PLAN.md`, update `STATE.md`, stop.
