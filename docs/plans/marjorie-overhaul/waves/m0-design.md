# Wave M0 — Design specs + charter amendment

Paste everything below this line into a fresh **Opus** session (`/model opus`) in Swift2.

---

You are executing Wave M0 of the Marjorie Overhaul (`docs/plans/marjorie-overhaul/PLAN.md`, epic #4180). Read `PLAN.md` in full, then `docs/agents/marjorie.md`, `docs/agents/runner-prompts/marjorie-brief.md`, `scripts/marjorie/assemble-brief.mjs`, `.github/workflows/watchdog.yml` (skim; it is ~1,100 lines), `scripts/watchdog/upsert-alert.sh`, `scripts/watchdog/send-mail.py`, `.github/workflows/brief-mailer.yml`, `.github/workflows/marjorie-inbox.yml`, `scripts/social/weekly-brief.mjs` (the Discord webhook pattern to copy), `scripts/knowledge-fb-upload.mjs`, `apps/web/app/api/feedback/route.ts`, `apps/web/app/api/intake/route.ts`, the `docs/decisions.md` entry dated 2026-09-12 "Three Discord channels, one job each", and the Hermes contract recorded on #4180. This is PM mode: specs, not code. Use `scout`/`researcher` for lookups; keep your own context for judgment.

**Hard rules carried in:** no `git restore`/`reset --hard`/`clean`; never run `scripts/social/post-queue.mjs` or `delete-media.mjs`; never hand-write an `approval` object; never merge a `social/queue/**.json` PR; `gh secret`/`gh variable` are human-only → `HUMAN-ACTIONS.md` (format v2, invoke the `human-actions` skill); you confirm real CI yourself before merging; "auto-merge armed" is not "merged" — check PR state before reporting; files under 300 lines; a `MAP.md` row for every new file. Marjorie's charter may not be edited by Marjorie; you are not Marjorie, but the amendment PR must carry a founder's approving comment before it merges — flag it in the PR body and do not merge it yourself.

**Settled, do not re-open:** channel roles; human actions filed by PR only; Kanban is unreachable from routines; agent processes never see `DISCORD_BOT_TOKEN`; email is retired except the backup receipt and a webhook-failure fallback.

## Deliverables

### PR 1 — specs, branch `docs/marjorie-overhaul-specs`

Five specs in `docs/specs/marjorie-overhaul/`, each with these sections in this order: **Behavior you will see** (for a non-coder founder, ≤200 words, concrete: what appears in Discord and when), **Data**, **Mechanics** (workflow/script/prompt changes with file paths), **Acceptance criteria** (testable), **Files affected**, **Open questions** (only genuine gaps; decide everything reversible yourself and say so).

1. `c1-delivery.md` — one module `scripts/marjorie/lib/discord.mjs`: `post(text, {thread?})`, chunking, mention-neutralising (reuse or extract from `scripts/community/discord-delivery.mjs`; do not write a second copy), the `ops` environment with `DISCORD_MARJORIE_WEBHOOK_URL`, and the fallback: a non-2xx from Discord, after one retry, sends the same text through `send-mail.py` with subject prefix `[discord failed]` — the only judgment-free path to email. Decide webhook vs bot token for posting (recommend webhook; say why) and what M4 needs to read thread replies (a poller job with the bot token, modelled on `social-approval-poll.yml`, never an agent step).
2. `c2-brief.md` — the Founders' Brief rebuilt. Sections in order: **Waiting on you** (every open `HUMAN-ACTIONS.md` number with its title and age, nothing else), **Since yesterday** (merges, alerts opened/closed, submissions in), **Today** (what routines will run, what she will do), **Site** (prod smoke, e2e, Vault Run freshness, content lanes), **Tree** (one line from `social/lessons.md` and the scorecard, read-only), **Distance to done** scored against the current Definition of Done in `CLAUDE.md` (eight items), not the retired 12-gate set. Cap 40 lines. Delivered by C1 at the same cron as today; the GitHub issue stays as the durable copy; `brief-mailer.yml` retired; founder replies in the Discord thread become comments on the issue (poller, M4 mechanics, but spec it here). Write the exact template with a filled example.
3. `c3-email-retired.md` — the inventory of every `send-mail.py` call site (`brief-mailer.yml`, `watchdog.yml`, `tree-mail.yml`, `social-poster.yml`, `production-backup.yml`) and what each becomes: Discord line in `#longlive-marjorie`, or (Tree's weekly plan mail) a line in `#longlive-tree` from Tree — check with the Tree plan that this does not add a non-approval post there; if it would, it goes to `#longlive-marjorie` as "Tree's plan PR is up". `production-backup.yml` keeps email. `marjorie-inbox.yml` is deleted (its job, relaying email replies, has no email to relay). Wyatt's CC disappears with the mail. Spell out the `MARJORIE_EMAIL` / `GMAIL_APP_PASSWORD` secrets that become unused and file a human action to remove them only after MR1 confirms nothing fired.
4. `w1-watchdog-handling.md` — `routine-marjorie-ops.yml` (via `routine-template.yml`, model Sonnet, `allowed_tools` scoped to `gh issue`, `gh run`, `gh workflow run`, `gh pr`, Read/Grep, and the C1 post script; never `gh variable`/`gh secret`). Trigger: `workflow_dispatch` from `watchdog.yml` right after `upsert-alert.sh open`, plus an hourly sweep of open `watchdog-alert` issues. A handler table, one row per alert `watchdog.yml` can raise (enumerate them all from the file): what she checks, what she may do herself (re-dispatch the quiet workflow, re-run a failed check, nudge a stuck PR within the existing re-run budget), what becomes a human action (v2 item via PR; literal steps), what she posts in-channel, and when she closes the alert. The FB export row: read `scripts/knowledge-fb-upload.mjs` and write the human action's Steps so a non-coder can do the export from Facebook's group settings to `npm run knowledge:fb-upload` — that text is a deliverable, not a TODO. `watchdog.yml` loses the email leg; `upsert-alert.sh` gains the Discord leg through C1. Include how a routine going dark is itself noticed (watchdog already watches `routine-*.yml`; confirm the new one is covered by the dynamic list).
5. `s1-triage.md` — `routine-marjorie-triage.yml` over issues labelled `intake` or filed by `/api/feedback`, `/api/intake`, `submit-link.ts`. Classification: bug / request / content correction / spam / needs-founder. Bug and content correction with enough evidence → an issue for the build desk with acceptance criteria and the reporter's words quoted; request → an issue labelled for the roadmap with her one-paragraph UX recommendation; needs-founder → in-channel question with her recommendation and a 7-day nudge; spam → closed with a label, never silently. She never edits product code or content. Include the label set and how a founder overrules in one reply.

Plus: one `docs/decisions.md` entry per expensive-to-reverse choice (delivery mechanism, brief as the single daily surface, triage authority), dated, referencing #4180. Update `docs/roadmap.md`'s ownership line for site ops.

### PR 2 — charter amendment, branch `docs/marjorie-charter-amendment`

Edit `docs/agents/marjorie.md`: mission becomes "the site runs and the user experience improves; she dispatches every fix and is accountable for the outcome"; add the responsibilities above; keep "never writes product code or content"; replace every email-based communication line with `#longlive-marjorie`; add a `## Channels` section mirroring the decisions entry; keep the `## Sampling rubric` heading and update its lines for the new duties (≤6 lines). Update `docs/agents/README.md`'s roster line. **Open the PR with "needs Joey's approving comment before merge" in the body and do not merge it.**

### Before opening PR 1

Ask the `architect` agent (Fable, read-only) for one review pass of the five specs against the goal state, budget 20 minutes; log the invocation in `STATE.md` under Architect invocations. Fold in what is right, note what you rejected and why in the PR body. If the weekly Fable floor is spent, skip this and say so.

## Done means

PR 1 merged with your own CI confirmation; PR 2 open with the founder flag; the FB export human-action text exists in `w1-watchdog-handling.md`; HA #66 status stated; `MAP.md` rows for the spec directory; tick M0 on #4180, update `STATE.md`, stop.
