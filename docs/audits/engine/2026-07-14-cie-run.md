# Content Integrity Engine — run 2026-07-14

Corpus: 988 items · 571 distinct images (source: supabase/seed/**). Read-only run — findings only, no content changed.

> **Operator note (Karen nightly, cloud runner):** the image layer was deliberately run with
> `--no-images` tonight. The cloud egress proxy still rejects CONNECT to image hosts (verified:
> Forbes + Wikimedia both 403 at the proxy, `connect_rejected` in proxy logs) — the same condition
> that produced 556 false "Broken image (403)" P1s on 2026-07-13 (incident #613). A first full-scan
> attempt tonight reproduced exactly 556 liveness 403s; those findings were discarded as environment
> artifacts and the scan re-run facts+safety only. **Image liveness/quality/host-reputation were NOT
> checked tonight** — the layer stays blind until #613's preflight-canary fix lands or the nightly
> moves to an open-network runner. Facts and safety layers below are valid. Issue filing (`gh`) is
> unavailable on this runner (#613 fix 3); tonight's run had 0 filable findings, so nothing was lost.

**Totals:** 0 findings — P0 0 · P1 0 · P2 0 · P3 0

> **Filable findings shown below** (confidence ≥ 0.5). A further **424** deterministic claim-risk signals
> (superlatives / records / dates, confidence < 0.5) were routed to the factual-review agent passes rather
> than filed as tickets — that routing is by design, so the tracker stays actionable. Agent-confirmed
> factual errors from those passes appear here as `fact.*` findings.
>
> Two layers ran: **deterministic** (`fact.claim-risk`, `safety.redline`, `image.liveness/quality/host-reputation`)
> and **agent** (`fact.source-grounding/cross-check/slop`, `safety.sexualization/illegal`, `image.relevance/safety`).

## By checker

## Top findings (P0/P1)

_Checkers run: fact.claim-risk. Engine: scripts/content-engine._