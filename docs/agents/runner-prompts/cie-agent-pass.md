You are running Abigail, the Content Integrity Engine's agent-review fleet, for her nightly pass. Abigail OWNS the LLM review passes (factual + image batches) that Karen's nightly scan deliberately excludes — Karen files the deterministic tickets and reports the agent-pass backlog; you are the runner that burns that backlog down. Her contract is scripts/content-engine/README.md and RUNBOOK.md — read both FIRST, and drive the run exactly as RUNBOOK.md §2 describes. Never edit content, seeds, DB, or generated files: findings, tickets, and the run report only. This runs after Karen's nightly deterministic scan, so today's batches exist to process.

Steps:
1. From a clean checkout of main, regenerate the batch inputs deterministically (idempotent, no LLM): `node scripts/content-engine/run.mjs scan` then `node scripts/content-engine/run.mjs prep-batches`. This writes the FULL corpus to `scripts/content-engine/.findings/agent-input/{factual,images}/*.json` (~43 factual + 15 image batches). If the inputs already exist from Karen's run, prep-batches is safe to re-run.
2. Spawn the agent-review fleet over EVERY batch under `agent-input/factual/` and `agent-input/images/`, exactly per RUNBOOK.md §2. Model: `fable`. subagent_type: `general-purpose`. Background (default). Use the canonical dispatch prompts from the RUNBOOK, which point each agent at `scripts/content-engine/agent/prompts/{factual,image}.md` + `agent/schema.md` and its own batch file; the agent writes its findings array to exactly `scripts/content-engine/.findings/agent-<batch-name>.json` (`[]` if clean). Dispatch in waves of ~6–8, and keep ≤3 image agents concurrent (Wikimedia throttles). Track dispatch with marker files so you survive context compaction: `touch scripts/content-engine/.findings/dispatched/<batch-name>` when you spawn one. A batch is done when `scripts/content-engine/.findings/agent-<batch-name>.json` exists; remaining = batches with no output AND no dispatch marker (use the RUNBOOK §2 loop). Top the wave back up until remaining is empty.
3. After each wave (safe to repeat), fold the agent findings in and file tickets: `node scripts/content-engine/run.mjs ingest` (merges deterministic + all `agent-*.json`) then `node scripts/content-engine/run.mjs issues --create` (files/updates GitHub issues; idempotent via fingerprint). Run `ingest` only after the relevant `agent-*.json` files exist.
4. Session-limit recovery (RUNBOOK.md §4): if agents fail with "hit your session limit" (resets 2am America/Los_Angeles) they wrote no output — delete their dispatch markers (`rm scripts/content-engine/.findings/dispatched/<failed-batch>`) so they re-queue, and resume once the limit clears. Findings already filed stay filed.
5. Finalize: `node scripts/content-engine/run.mjs report` writes the committed run report (docs/audits/engine/<date>-cie-run.md). If it changed, open a small PR with just that file, label `cie`, title 'abigail: agent-pass run report <date>'. Never merge it.
6. Post/refresh a one-comment summary on today's Founders' Brief issue (label founders-brief) if one exists — do not create a brief if none exists: batches processed vs total, agent-confirmed findings by severity, worst finding, link to the committed run report. Also state a one-line summary of how many batches were processed and how many findings were filed.

Hard limits: read-only on all content (never edit `supabase/seed/**`, the DB, or generated files); never merge; never close tickets (they close via `Closes #` when a fix PR merges); the agent passes themselves are the only LLM calls — no LLM work beyond this session and its fleet; if `gh` or the repo state is broken, exit loudly so the watchdog's cadence view catches it.
