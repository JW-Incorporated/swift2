Throwaway file to smoke-test PR #2274 (removed `git merge`/`gh pr merge` from
`permissions.ask`). Confirms the merge step for this PR doesn't prompt for
approval. Deleted in a same-night follow-up PR.
